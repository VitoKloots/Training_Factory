{% extends 'base.html.twig' %}

{% block Title %}Training{% endblock %}

{% block show %}
    <h1>Training</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Naam</th>
                <td>{{ training.naam }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ training.description }}</td>
            </tr>
            <tr>
                <th>Duur</th>
                <td>{{ training.duur }}</td>
            </tr>
            <tr>
                <th>Kosten</th>
                <td>{{ training.kosten }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('aanbod') }}">back to list</a>

    <a href="{{ path('admin_training_edit', {'id': training.id}) }}">edit</a>

    {{ include('admin/training/_delete_form.html.twig') }}
{% endblock %}
