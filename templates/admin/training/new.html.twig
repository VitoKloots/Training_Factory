{% extends 'base.html.twig' %}

{% block title %}New Training{% endblock %}

{% block newTraining %}
    <h1>{{ title }}</h1>

    {{ include('admin/training/_form.html.twig') }}

    <a href="{{ path('admin_training_index') }}">back to list</a>
{% endblock %}
